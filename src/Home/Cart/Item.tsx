import { View, Text } from "react-native";
import React from "react";

interface ItemProps {}

const Item = ({ Item }: ItemProps) => {
  return (
    <View>
      <Text>Item</Text>
    </View>
  );
};

export default Item;
